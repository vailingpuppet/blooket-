var f = document.createElement('iframe');f.style.display = 'none';document.body.appendChild(f);window.prompt = f.contentWindow.prompt;(() => {  var hack = Object.values(document.querySelector('#app > div > div'))[1]    .children[1]['_owner'].stateNode.state;  if (hack.myCard) {    var u_prompt = window.prompt("What values do you want to set the enemy's card to?");    setInterval (function () {        hack.enemyCard.strength = u_prompt;        hack.enemyCard.wisdom = u_prompt;        hack.enemyCard.charisma = u_prompt;    });  }})();
